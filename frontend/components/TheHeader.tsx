import { Typography } from "@mui/material";

const TheHeader = () => {
  return (
    <Typography variant="h4" sx={{ maxHeight: "60px", textAlign: "center" }}>
      Мои заметки
    </Typography>
  );
};

export { TheHeader };
